# App Properties

# TODO: Update APP_SECRET
# APP_SECRET is a random 32 character string that can be generated
# via running the following command in a terminal: openssl rand -base64 32
# It is is used to decrypt the JWT passed by the frontend, therefore it should match the frontend variable: APP_JWT_SECRET
APP_SECRET=
APP_PORT="4000"

# Anonymous Feedback Database Config
# The docker-compose file includes a postgres container, so the connection details are commented out
# unless there's a need to connect to a different database
# DB_FEEDBACK_USER=
# DB_FEEDBACK_PASSWORD=
# DB_FEEDBACK_DATABASE=
# DB_FEEDBACK_HOST=
# DB_FEEDBACK_SCHEMA=
DB_FEEDBACK_POOL_MAX=1


# Canvas API
API_CANVAS_URL="https://<TEST_INSTANCE>.instructure.com"
# TODO: Update API_CANVAS_TOKEN
# API_CANVAS_TOKEN is generated by a Canvas Admin
API_CANVAS_TOKEN=

# Enable Debug
DEBUG=*

# These are the basic auth credentials used to call GET /feedback/emailReminder
API_USER="<USER>"
API_PASSWORD="<PASSWORD>"

MAIL_HOST="<MAIL_HOST>"
MAIL_FROM="<MAIL_ADDRESS>"
